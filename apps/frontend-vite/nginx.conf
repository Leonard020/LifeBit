server {
  listen 80;
  server_name localhost;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # API 프록시 설정은 여기에 추가할 수 있지만,
  # 우리는 모든 서비스가 같은 Docker 네트워크에 있으므로
  # 최상위 Nginx 리버스 프록시에서 처리하는 것이 더 효율적입니다.
} 